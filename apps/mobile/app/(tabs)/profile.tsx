/**
 * Profile tab → PROF-01 (Settings root). The full settings UI is the
 * SettingsRoot screen; the tab renders it directly, and its rows push the
 * edit sub-screens in the app/settings stack (Step 20).
 */
export { default } from '../settings/index'
