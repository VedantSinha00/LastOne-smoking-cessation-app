# Dependencies
node_modules/
jspm_packages/
web_modules/

# Turbo and Build Cache
.turbo/
.claude/
*.tsbuildinfo

# Build/Compilation outputs
dist/
build/
.expo/
out/
.next/

# Environment files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Firebase service-account key (used by EAS to upload builds to App Distribution).
# SECRET — must never be committed. Each machine keeps its own local copy.
firebase-sa.json
**/firebase-sa.json
google-services.json

# OS Metadata
.DS_Store
Thumbs.db

# MS Office lock/temp files
~$*

# Local Session Notes
session_notes.md

# Supabase CLI local link state (machine-specific; not the migrations)
supabase/.temp/
supabase/.branches/

# Graphify outputs and cache
graphify-out/
